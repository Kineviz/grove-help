{"blocks":[{"type":"codeTool","data":{"codeData":{"value":"md`# 🤔 How to: use libraries that need CSS\n\nSo you really want to use a library, but importing it in Observable only includes the JavaScript code and not the stylesheets. What do you do? In most cases, there's a fix: fetching the required CSS and putting it on the page with the \\`html\\` tagged template literal. Let's use [Chartist](http://gionkunz.github.io/chartist-js/index.html) as an example: it's a library that generates SVG-based charts, and uses a bunch of default CSS to style the elements. If you just include the library and create a chart, it looks pretty borked: you need the CSS.\n\n1. Figure out where the CSS is, by going to \\`https://unpkg.com/modulename\\`\n2. Insert that CSS text on the page in a \\`<link>\\` tag in html.`","pinCode":false}}},{"type":"codeTool","data":{"codeData":{"value":"md`<svg width=${width} height=${width*0.625} style='background-color:#000'>\n  <rect width=${width/2} height=${width*0.5} transform='translate(${width * 0.075}, ${width * 0.075})' stroke='#fff' fill='none' stroke-width=3 />\n  <g transform='translate(${width * 0.085}, ${width * 0.085})'>\n    <text alignment-baseline='hanging' fill='#fff' style='font-weight:200;font-family:Helvetica, sans-serif;font-size:${width / 6};'>CSS</text>\n    <text alignment-baseline='hanging' y=${width * 0.17} fill='#fff' style='font-weight:200;font-family:Helvetica, sans-serif;font-size:${width / 6};'>IS</text>\n    <text alignment-baseline='hanging' y=${width * 0.34} fill='#fff' style='font-weight:200;font-family:Helvetica, sans-serif;font-size:${width / 6};'>AWESOME</text>\n  </g>\n</svg>`","pinCode":false}}},{"type":"codeTool","data":{"codeData":{"value":"stylesheet = (html`<link rel='stylesheet'\n  href='https://unpkg.com/chartist@0.11.0/dist/chartist.min.css' />`)","pinCode":false}}},{"type":"codeTool","data":{"codeData":{"value":"md`Then we can use Chartist just like we'd use any other module.`","pinCode":false}}},{"type":"codeTool","data":{"codeData":{"value":"{\n  let container = DOM.element('div');\n  container.style.height = `${width / 1.6}px`;\n  new Chartist.Line(container, {\n    labels: [1, 2, 3, 4, 5, 6, 7, 8],\n    series: [\n      [1, 2, 3, 1, -2, 0, 1, 0],\n      [-2, -1, -2, -1, -2.5, -1, -2, -1],\n      [0, 0, 0, 1, 2, 2.5, 2, 1],\n      [2.5, 2, 1, 0.5, 1, 0.5, -1, -2.5]\n    ]\n  }, {\n    high: 3,\n    low: -3,\n    showArea: true,\n    showLine: false,\n    showPoint: false,\n    fullWidth: true,\n    axisX: {\n      showLabel: false,\n      showGrid: false\n    }\n  });\n  return container;\n}","pinCode":false}}},{"type":"codeTool","data":{"codeData":{"value":"Chartist = (require('chartist'))","pinCode":false}}}],"version":"2.19.1"}